package group22.GrammarClasses;

import group22.DataType;
import group22.ScopeManager;
import provided.JottTree;

import java.util.HashMap;

public class JottValidator {
    public static boolean isJottValid(JottTree jottTree) {
        HashMap<String, DataType> createdFunctions = new HashMap<>();
        HashMap<String, HashMap<String, DataType>> createdVariables = new HashMap<>();
        String currentScope = "";

        //
    }
}
